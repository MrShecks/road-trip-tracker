<?xml version="1.0" encoding="utf-8"?>

<!--
    FIXME: I can't seem to get the collapsing toolbar to work well with the BottomNavigationView
    FIXME: When I use the CoordinatorLayout to enable the collapsing toolbar and have the
    FIXME: BottomNavigationView hide when the contents of the fragment host scroll it results
    FIXME: in either the top or bottom of the fragment content being overlapped.
-->

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".view.activities.MainActivity"
    >

    <include layout="@layout/main_toolbar"/>

    <fragment
        android:id="@+id/navHostFragment"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        app:defaultNavHost="true"
        app:navGraph="@navigation/main_navigation" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigationView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"

        app:menu="@menu/main_navigation_menu"
        app:layout_behavior="@string/hide_bottom_view_on_scroll_behavior" />

</LinearLayout>